# 数字内容过滤课程设计：四个实验说明（Agent 替代 PPT 版）

> 本文档根据《数字内容过滤课程设计（3.1）》PPT整理，目标是让后续 agent 不再依赖原 PPT，也能理解四个实验的任务边界、实现目标、核心原理、输入输出、验收要求和报告撰写要点。  
> 文档重点覆盖：实验一 Web 信息获取、实验二 文本倾向理解、实验三 基于内容的电子邮件过滤、实验四 基于内容的不良短消息过滤。

---

## 0. 课程设计总体定位

数字内容过滤的核心问题是：面对互联网中快速增长的文本、图片、视频、邮件、短信等数字内容，如何通过自动化方法识别、分类、拦截或标记不良信息，降低人工审核成本，并提升过滤效率与准确性。

本课程设计的四个实验围绕“内容获取 → 文本理解 → 场景化过滤 → 终端应用验证”展开：

1. **实验一：Web 信息获取**  
   学习如何主动从 Web 获取数字内容，为后续分类和过滤系统准备语料。

2. **实验二：文本倾向理解**  
   学习如何对中文文本进行分词、特征表示、特征选择和倾向性分类，形成基础文本分类器。

3. **实验三：电子邮件过滤**  
   在实验二分类能力的基础上，将文本分类方法用于垃圾邮件和正常邮件识别。

4. **实验四：不良短消息过滤**  
   在实验二分类能力的基础上，将文本分类方法用于短信内容过滤，并建议在安卓模拟器中完成客户端验证。

四个实验之间不是孤立关系。较合理的工程路线是：

```text
实验一：爬取或采集文本数据
        ↓
实验二：构造训练集/测试集，训练文本倾向分类器
        ↓
实验三：将分类器应用到邮件语料，识别垃圾邮件
        ↓
实验四：将分类器应用到短信语料，在终端或模拟器中验证过滤效果
```

---

## 1. 通用提交与验收要求

PPT 最后给出的提交材料与验收方式如下：

- 提交材料：
  - 实验报告文档；
  - 实验程序源代码文件夹；
  - 将文档和代码整体打包成一个文件。
- 命名方式：
  - 以学生姓名命名压缩包。
- 提交方式：
  - 发送至教师邮箱，PPT 中给出的邮箱为 `sunjie@bupt.edu.cn`。
- 验收方式：
  - 验收时间和地点待定；
  - 每组同学需要带笔记本电脑验收；
  - 现场应能够展示程序运行效果、实验结果和关键参数。

后续 agent 在帮助完成实验时，应优先保证：

1. **有可运行代码**，不是只有报告。
2. **有输入数据和输出结果**，能现场展示。
3. **有参数说明和结果分析**，便于写实验报告。
4. **代码结构清晰**，能说明数据从哪里来、如何处理、如何分类、如何评估。
5. **实验边界明确**，不要把四个实验混成一个大而全系统；可以复用模块，但报告与演示应分实验说明。

---

## 2. 实验一：Web 信息获取的学习和实践

### 2.1 实验性质

实验一是一个**验证型实验**，要求通过编程实现一个简单的网络爬虫，并验证其功能和相关参数。

它是后续所有实验的基础，因为 Web 信息获取是 Web 信息处理的第一步。只有先获取文本、评论、网页或其他数字内容，后续才能进行文本分类、倾向理解、邮件过滤或短信过滤。

### 2.2 实验目的

本实验的目标包括：

- 认识信息获取技术在数字内容过滤系统中的作用；
- 学习网络爬虫的基本概念、基础知识和工作原理；
- 掌握针对 Web 主动获取数字内容信息的基本技术；
- 能够在 Windows 环境下设计并实现一个简单的 Web 数字内容获取程序；
- 初步模拟“从 Web 信息定位到真正获取内容并保存”的过程。

### 2.3 核心原理

网络爬虫又称 spider、网页蜘蛛、网络机器人或网页追逐者，本质上是一类自动搜索程序。它会访问网页，解析网页中的链接或目标内容，并将获取到的网页或文件以副本形式保存到本地。

实验一中的爬虫可以理解为一个简单的数据流水线：

```text
目标入口页 / URL 列表
        ↓
发送 HTTP 请求
        ↓
解析 HTML 或读取页面文本
        ↓
提取目标链接或目标内容
        ↓
逐个访问目标 URL
        ↓
保存页面、文本或文件到本地目录
```

PPT 中将该过程归纳为三个核心步骤：

#### 2.3.1 获取 URL 列表

爬虫首先需要知道要访问哪些网页或文件。URL 来源可以有两种：

1. **手动准备 URL 列表文本文件**  
   每行写一个目标网页 URL，程序逐行读取后下载。

2. **从入口页面自动提取链接**  
   先访问一个目录页、列表页或索引页，然后解析 HTML 中的 `<a>` 标签，提取 `href` 属性，得到目标 URL 列表。

推荐现代实现方式：

- 使用 `requests` 发送 HTTP GET 请求；
- 使用 `BeautifulSoup` 或 `lxml` 解析 HTML；
- 使用 `urllib.parse.urljoin` 将相对链接转换为绝对链接；
- 对 URL 去重，避免重复下载同一页面。

#### 2.3.2 下载文件或网页内容

获得 URL 列表后，程序逐个访问链接：

- 对文本网页或小说章节，可以读取 `response.text`；
- 对图片、PDF 等二进制文件，可以读取 `response.content`；
- 对评论、标题、正文等结构化内容，可以在 HTML 中进一步定位指定标签。

本课程设计更关注后续文本分类，因此实验一更建议采集**文本类内容**，例如：

- 书评；
- 电影评论；
- 新闻评论；
- 商品评论；
- 博客文章；
- 论坛公开帖子；
- 公开网页中的文章标题和正文。

#### 2.3.3 本地持久化存储

下载到的内容需要保存到本地目录。程序应完成：

- 自动创建保存目录；
- 为每个页面生成合法文件名；
- 将网页或文本保存为 `.html`、`.txt`、`.json`、`.csv` 等格式；
- 保存必要的元数据，例如原始 URL、标题、采集时间、编码、分类标签等。

推荐保存结构：

```text
data/
  raw_pages/
    page_0001.html
    page_0002.html
  texts/
    item_0001.txt
    item_0002.txt
  metadata.csv
```

其中 `metadata.csv` 可以包含：

```text
id,url,title,local_file,category,crawl_time,status_code
```

### 2.4 知识学习要求

PPT 中要求学习以下内容：

- HTML 背景知识；
- 基本 HTML 标记：
  - 标题；
  - 段落；
  - 字体；
  - 颜色；
  - 表格；
  - 图片；
  - 链接；
- 一种高级语言编程技术，可选：
  - Python；
  - ActivePERL；
  - Java Eclipse；
  - Microsoft Visual Studio 等；
- Windows 网络编程技术：
  - 建立连接；
  - 发送文件请求；
  - 接收响应；
  - 关闭连接；
- 基本文件读写技术：
  - 打开已有文件；
  - 给文件命名；
  - 自动生成新文件；
  - 写入文件内容。

后续 agent 若采用现代 Python 实现，可将“Windows 网络编程”落实为：

- `requests.Session()` 管理连接；
- `requests.get()` 发送请求；
- `pathlib` / `os` 管理目录；
- `open(..., encoding="utf-8")` 写入文本文件；
- `csv` / `json` 保存结构化元数据。

### 2.5 动手实践要求

PPT 对实验一的实践要求是：

1. 设计一个目标网页 URL 列表文本文件；
2. 利用 Windows 网络编程技术实现一个简单爬虫；
3. 爬虫能够自动获取 URL 列表文本文件中的所有页面；
4. 将获取到的页面保存在本地目录下；
5. 用 IE 或浏览器打开自动获取的文件，检查是否正确显示；
6. 如果显示不正确，需要继续修改程序，直到能正确显示。

如果用 Python 完成，可以将“IE 检查显示”替换为：

- 用浏览器打开保存的 `.html` 文件；
- 检查 `.txt` 文件是否乱码；
- 检查 `metadata.csv` 中是否记录了每条数据；
- 随机抽样若干条，确认内容和 URL 对应正确。

### 2.6 编码问题与异常处理

PPT 特别强调网页编码问题。网页可能使用：

- UTF-8；
- GBK；
- GB2312；
- Big5；
- ISO-8859-1；
- 服务器返回编码和 HTML 声明编码不一致的情况。

程序应尽量处理乱码：

- 检查 `response.encoding`；
- 必要时使用 `response.apparent_encoding`；
- 保存文件时显式指定编码，例如 `utf-8`；
- 对中文网站，如果出现乱码，尝试 GBK / GB18030；
- 对异常页面记录日志，不要让整个爬虫中断。

建议 agent 实现日志字段：

```text
url,status_code,success,error_message,local_file
```

### 2.7 反爬拦截识别与合法边界

PPT 中列出了常见被拦截特征，后续 agent 应理解这些现象：

- 浏览器能打开，但代码、curl、Postman 访问失败；
- 返回 403 Forbidden；
- 返回 429 Too Many Requests；
- 返回 401、405 等异常状态码；
- 返回空白页、验证码页、访问受限提示页；
- 请求几次后中断，换 IP 或换浏览器又能访问；
- 跳转到登录页、验证码页；
- 真实浏览器能访问，但自动化浏览器或无头模式不能访问。

课程实验应遵守合法边界：

- 只采集公开数据；
- 遵守网站 `robots.txt` 和网站服务条款；
- 不采集隐私数据；
- 不绕过登录、验证码、付费墙或访问控制；
- 不进行高频请求；
- 不攻击、不撞库、不破坏网站服务；
- 如遇强反爬或访问限制，优先更换更适合教学实验的公开数据源。

安全、合规、适合教学实验的处理方式包括：

- 设置合理 `User-Agent`；
- 控制请求频率；
- 使用随机延时；
- 使用同一个 `Session` 维持正常会话；
- 对失败请求设置重试上限；
- 遇到验证码、登录限制或访问受限时停止采集。

### 2.8 推荐实现目标

为了便于后续实验复用，实验一的推荐实现目标是：

- 输入：
  - `urls.txt`：目标 URL 列表；
  - 或一个入口 URL，用于自动提取链接。
- 输出：
  - 本地网页文件；
  - 纯文本文件；
  - 元数据表；
  - 爬取日志。
- 关键功能：
  - URL 读取；
  - HTTP 请求；
  - HTML 解析；
  - 正文或评论提取；
  - 编码处理；
  - 文件保存；
  - 异常处理；
  - 简单去重。
- 验证方式：
  - 打开保存的文件能正常显示；
  - 文本内容无明显乱码；
  - 本地目录中有实际采集结果；
  - 日志能显示成功和失败情况。

### 2.9 实验报告要点

实验一报告需要说明：

- 网络爬虫的设计和实现过程；
- 目标网站或目标 URL 列表来源；
- 程序整体流程；
- 关键代码模块：
  - URL 读取；
  - 请求发送；
  - 页面解析；
  - 文件保存；
  - 编码处理；
  - 异常处理；
- 遇到的问题：
  - 编码乱码；
  - 请求失败；
  - 页面结构变化；
  - 反爬限制；
  - 文件命名冲突；
- 解决方法；
- 最终效果：
  - 成功下载了多少页面；
  - 保存到哪个目录；
  - 是否能打开；
  - 是否能为实验二提供可用语料。

---

## 3. 实验二：文本倾向理解的学习和实践

### 3.1 实验性质

实验二是一个**验证型实验**，要求通过编程实现一个简单的文本倾向分类系统，并验证其功能和相关参数。

实验二是整个课程设计中最核心的算法实验。实验三和实验四都可以在实验二的分类器基础上扩展到邮件和短信场景。

### 3.2 实验目的

本实验目标包括：

- 掌握文本分类的基本技术；
- 掌握文本倾向理解的基本技术；
- 认识文本倾向理解技术在数字内容过滤系统中的作用；
- 学习中文文本预处理、分词、向量化、特征选择和分类评估；
- 编程实现一个简单的中文文本倾向分类器；
- 通过调整特征阈值和参数，观察分类性能变化。

### 3.3 实验条件

PPT 中给出的实验条件为：

- 硬件：
  - 每人一台 PC 机。
- 软件：
  - Windows XP；
  - IE；
  - Notepad 或类似文本编辑器；
  - ActivePERL；
  - Java Eclipse；
  - Microsoft Visual Studio。
- 网络：
  - 每台 PC 通过 100M 端口连接外部 Internet。

现代实现可以等价替换为：

- Windows 10 / Windows 11；
- Python 3；
- 中文分词库，如 `jieba`；
- 机器学习库，如 `scikit-learn`；
- 文本存储格式，如 `.txt`、`.csv`、`.json`；
- Jupyter、PyCharm、VS Code 或命令行均可。

### 3.4 核心原理

#### 3.4.1 文本分类与数字内容过滤

对海量文本进行分类，是组织和处理文本信息的重要方法。数字内容过滤本质上也是一种分类问题，即判断一段文本是否属于某一类需要过滤或标记的内容。

传统文本分类通常关注文本的客观主题，例如：

- 体育；
- 财经；
- 科技；
- 娱乐；
- 暴力；
- 色情；
- 广告。

而文本倾向分类关注文本中表达出的主观态度，例如：

- 支持；
- 反对；
- 褒奖；
- 批评；
- 中性；
- 宣扬；
- 抵制。

#### 3.4.2 主题过滤与倾向性过滤

PPT 中强调，不良信息过滤可以分为两类：

1. **主题过滤**  
   根据文本讨论的话题进行过滤。例如，只要文本属于“暴力”“色情”等明显不良主题，就可以过滤。

2. **倾向性过滤**  
   对敏感话题不能只看是否出现相关主题，还要看作者观点。  
   例如，同样讨论邪教：
   - 宣扬邪教的内容应过滤；
   - 反对邪教、批判邪教的内容不应过滤。

因此，倾向性过滤比普通主题分类更依赖主观语义、评价词、情感词、否定词、程度副词以及上下文。

#### 3.4.3 向量空间模型 VSM

实验二要求学习中文文本常用表示方法：**向量空间模型 VSM（Vector Space Model）**。

基本思想是：

```text
文本 → 分词 → 特征词集合 → 数值向量
```

常见表示方式：

1. **词袋模型 Bag-of-Words**
   - 统计每个词在文本中出现的次数；
   - 不考虑词序；
   - 简单易实现。

2. **TF-IDF**
   - TF 表示词在当前文档中出现的重要程度；
   - IDF 表示词在整个语料库中的区分能力；
   - 高频但无区分度的词权重较低；
   - 有助于筛掉“的、是、了”等常见词。

TF-IDF 常见形式：

```text
tfidf(t, d) = tf(t, d) × idf(t)
idf(t) = log(N / df(t))
```

其中：

- `t` 是词项；
- `d` 是文档；
- `N` 是文档总数；
- `df(t)` 是包含词项 `t` 的文档数量。

#### 3.4.4 中文分词

中文文本没有天然空格分隔，因此需要中文分词。分词会影响特征质量。

实验中需要理解：

- 为什么中文需要分词；
- 分词错误会如何影响分类结果；
- 停用词如何去除；
- 领域词典如何改善分词；
- 短文本中分词稀疏问题如何影响分类。

可用工具：

- `jieba`；
- `pkuseg`；
- `HanLP`；
- 课程提供的中文分词模块。

#### 3.4.5 特征选择

PPT 强调特征向量选择对分类性能有很大影响。需要学习：

- 文档频率；
- 文档倒排频率；
- 停用词；
- 通过阈值控制特征数量；
- 倾向性特征的选择和表示。

实验中可以通过以下参数控制特征：

- 最小文档频率 `min_df`；
- 最大文档频率 `max_df`；
- 最大特征数 `max_features`；
- 是否使用二元词组 `ngram_range=(1, 2)`；
- 是否使用情感词典或倾向性单元列表；
- 是否保留否定词和程度副词。

#### 3.4.6 朴素贝叶斯分类器

PPT 要求学习和实践典型分类算法：**Naïve Bayes（朴素贝叶斯）**。

朴素贝叶斯的基本流程：

```text
输入文本
  ↓
分词和预处理
  ↓
向量化表示
  ↓
计算先验概率和条件概率
  ↓
计算每个类别的后验概率
  ↓
选择后验概率最大的类别
```

分类决策形式：

```text
预测类别 = argmax_c P(c) × ∏ P(x_i | c)
```

其中：

- `c` 是类别；
- `x_i` 是文本中的第 i 个特征；
- `P(c)` 是类别先验概率；
- `P(x_i | c)` 是特征在类别 c 下出现的条件概率。

实际实现中通常使用对数概率，避免大量小概率相乘导致数值下溢：

```text
score(c) = log P(c) + Σ log P(x_i | c)
```

PPT 还提到拉普拉斯平滑，用来解决某些词在某类中频率为 0 的问题：

```text
P(w | c) = (count(w, c) + α) / (total_words(c) + α × |V|)
```

其中：

- `α` 是平滑系数；
- `|V|` 是词表大小。

### 3.5 倾向性分类类别

PPT 建议选择一个具体主题，例如：

- 电影评论；
- 书评；
- 商品评论；
- 课程评价；
- 新闻评论。

然后实现一个评论文本集的倾向分类器。

倾向性分类分为三类：

1. **正面褒奖类**
   - 对描述对象持肯定语气；
   - 有明显称赞、表扬、赞叹；
   - 也可包括哀悼、惋惜等带有明确情感倾向的表达。

2. **负面批评类**
   - 对描述对象持否定、质疑、讽刺语气；
   - 有鄙视、批评、痛斥等色彩。

3. **中性类**
   - 叙述客观公正；
   - 无明显情感色彩；
   - 不包含明确个人评价。

### 3.6 实验步骤

PPT 将实验步骤分为三部分。

#### 3.6.1 感性认识

需要完成：

- 阅读两类不同文章，观察人们将其分为不同类别的依据；
- 讨论文本分类的依据；
- 讨论特征向量对分类结果的影响；
- 阅读同一主题下不同观点的评论文章；
- 观察不同倾向性类别的区分依据；
- 讨论倾向性特征可能如何选择与表示；
- 比较倾向性分类与主题分类的区别和联系。

这一阶段的作用是帮助理解：分类不是只看关键词，还要看词语组合、语气、否定、上下文和评价方向。

#### 3.6.2 知识学习

需要学习：

- 中文分词模块使用；
- 文本表示通用方法：向量空间模型；
- 传统文本分类方法：Naïve Bayes；
- Naïve Bayes 的参数估计；
- 文档频率和倒排文档频率；
- 通过阈值选择特征、控制特征数量；
- Naïve Bayes 算法模块使用；
- 分类性能评估方法；
- 倾向性特征的选择和表示。

#### 3.6.3 动手实践

PPT 对实践要求较具体，可以拆成两条线：

##### A. 主题分类线

1. 基于实验一爬虫构造主题分类训练语料和测试语料；
2. 基于向量空间模型实现文本表示；
3. 用一种高级编程语言实现 Naïve Bayes 分类器；
4. 对测试语料进行分类；
5. 计算准确率；
6. 分析分类错误原因；
7. 调整文档频率、倒排文档频率等阈值；
8. 多轮实验，确定较好的特征集；
9. 得到较优分类性能。

##### B. 倾向性分类线

1. 基于实验一爬虫构造特定主题评论文章语料；
2. 划分训练集和测试集；
3. 增加倾向性特征选择和表示；
4. 重新训练 Naïve Bayes 分类器；
5. 对测试语料分类；
6. 根据分类性能调整参数；
7. 多轮实验后确定较好的倾向性特征集；
8. 得到较优倾向分类性能。

### 3.7 评价指标

PPT 提到需要通过实验结果选择合适阈值，得到较好的：

- 准确率；
- 召回率；
- 综合 F 指数。

建议报告至少包含：

```text
Accuracy = 正确预测样本数 / 总样本数

Precision = TP / (TP + FP)

Recall = TP / (TP + FN)

F1 = 2 × Precision × Recall / (Precision + Recall)
```

对于三分类任务，应提供：

- 每一类的 precision / recall / F1；
- 宏平均 macro avg；
- 加权平均 weighted avg；
- 混淆矩阵。

### 3.8 推荐实现目标

实验二推荐实现为一个可复用文本分类模块：

```text
text_filter/
  data/
    train.csv
    test.csv
  src/
    preprocess.py
    train.py
    predict.py
    evaluate.py
  models/
    nb_model.pkl
    vectorizer.pkl
  outputs/
    metrics.json
    confusion_matrix.png
```

推荐输入数据格式：

```text
text,label
"这本书写得很好，情节非常吸引人",positive
"内容一般，没有什么特别之处",neutral
"剧情混乱，人物也很单薄",negative
```

推荐输出：

- 训练好的模型；
- 特征词表；
- 测试集预测结果；
- 准确率、召回率、F1；
- 混淆矩阵；
- 错误样本分析。

### 3.9 PPT 中 BS 模型与朴素算法部分的理解

PPT 中还出现了“结合 BS 模型与朴素算法”的说明，可以理解为一种分层过滤系统设计思想：

- 顶层是中心广播层，负责规则或模型更新；
- 底层是分布式监督节点，内置朴素贝叶斯分类器；
- 原始文本经过预处理、分词、去停用词后进入分类器；
- 使用拉普拉斯平滑解决零频问题；
- 可通过交叉验证优化超参数；
- 优点是响应快、效率高、适合大规模文本；
- 局限是朴素贝叶斯的特征独立性假设不总成立，对隐喻、讽刺等复杂语义处理较弱。

对后续 agent 来说，除非用户明确要求实现 BS 架构，否则实验二的主任务仍应聚焦于：

```text
中文文本预处理 + 特征表示 + 朴素贝叶斯分类 + 性能评估
```

### 3.10 实验报告要点

实验二报告需要说明：

- 文本倾向理解系统设计和实现过程；
- 语料来源；
- 类别定义；
- 数据集划分方式；
- 中文分词方法；
- 停用词处理方法；
- 文本向量化方法；
- 特征选择阈值；
- Naïve Bayes 分类原理；
- 训练过程；
- 测试结果；
- 准确率、召回率、F1；
- 参数调整过程；
- 分类错误原因分析；
- 实验效果总结。

---

## 4. 实验三：基于内容的电子邮件过滤

### 4.1 实验性质

实验三是一个**验证型实验**，要求通过编程实现一个简单的电子邮件过滤系统，并验证其功能和相关参数。

该实验是实验二在“邮件过滤”场景下的应用。

### 4.2 实验目的

本实验目标包括：

- 学习邮件内容过滤的基本技术；
- 掌握垃圾邮件识别的基本方法；
- 认识邮件过滤技术在数字内容过滤系统中的作用；
- 设计一个基于内容的邮件过滤系统；
- 实现垃圾邮件与正常邮件两类语料的划分；
- 能够在邮件服务器端或模拟服务器端完成自动识别、监控、处理或拦截垃圾邮件。

### 4.3 实验条件

PPT 给出的实验条件：

- 硬件：
  - 每人一台 PC 机；
  - 一台邮件服务器。
- 软件：
  - Windows XP；
  - IE7.0；
  - Notepad 或类似文本编辑器；
  - ActivePERL 5.6；
  - Java Eclipse 3.2；
  - Microsoft Visual Studio 2008。
- 网络：
  - 每台 PC 通过 100M 端口连接外部 Internet。

现代实现可替换为：

- Python 3；
- 本地模拟邮件文件；
- `.eml` 邮件文件；
- CSV 邮件数据集；
- 简单 Flask/FastAPI 模拟服务端；
- 或使用本地目录模拟“邮件服务器收件箱”。

### 4.4 背景与问题定义

电子邮件快捷、方便、高效，是现代通信的重要组成部分。但它也是容易受到非法信息污染的渠道之一，垃圾邮件泛滥会干扰用户工作与生活。

PPT 对垃圾邮件的大致定义包括：

- 收件人事先没有提出要求或同意接收的广告、电子刊物、宣传品等；
- 收件人无法拒收的邮件；
- 隐藏发件人身份、地址、标题等信息的邮件；
- 含有虚假信息源、发件人、路由等信息的邮件。

因此，垃圾邮件过滤既可以依靠发件人、路由、行为等信息，也可以依靠邮件正文内容。本实验重点是**基于内容的邮件过滤**。

### 4.5 传统邮件过滤技术

PPT 中列出传统邮件过滤技术包括：

1. **基于黑名单的过滤**
   - 根据发件人地址、域名、IP 等黑名单拦截；
   - 简单直接；
   - 容易被更换发件地址绕过。

2. **基于特征的过滤**
   - 根据邮件头、发件频率、链接数量、附件类型等特征判断；
   - 可结合内容特征使用。

3. **基于关键词的过滤**
   - 维护垃圾邮件常见词表；
   - 适合明显广告、诈骗文本；
   - 容易误判，且对变形词、同义词、规避写法不敏感。

4. **基于内容的过滤**
   - 分析主题、正文、链接、附件说明等内容；
   - 可使用朴素贝叶斯、SVM、神经网络等分类方法；
   - 是本实验核心。

5. **基于行为识别技术的过滤**
   - 根据发送行为、群发特征、频率异常等识别垃圾邮件；
   - 更接近真实邮件系统，但本实验不要求深入实现。

### 4.6 实验要求

PPT 的核心要求是：

> 设计一个简单的基于内容的邮件过滤系统，实现垃圾邮件和正常邮件两类语料的划分，并能够在邮件服务器端自动识别、监控、处理甚至拦截垃圾邮件。

对后续 agent 来说，应将该要求拆成可实现模块：

```text
邮件输入
  ↓
解析邮件内容
  ↓
提取主题和正文
  ↓
文本预处理
  ↓
调用分类器
  ↓
输出 spam / ham
  ↓
执行处理动作：标记、隔离、删除、拦截或放行
```

其中：

- `spam` 表示垃圾邮件；
- `ham` 表示正常邮件。

### 4.7 实验步骤

PPT 将实验三分为三个阶段。

#### 4.7.1 感性认识

需要完成：

- 采集一些垃圾邮件和正常邮件；
- 阅读并观察它们的差异；
- 讨论人们将邮件判定为垃圾邮件或正常邮件的依据；
- 讨论基于内容划分垃圾邮件和正常邮件的依据；
- 讨论垃圾邮件可能的处理方式。

可观察特征包括：

- 是否含大量广告词；
- 是否诱导点击链接；
- 是否包含虚假优惠、中奖、贷款、博彩等内容；
- 是否称呼泛化；
- 是否包含大量营销话术；
- 是否标题夸张；
- 是否包含异常链接；
- 是否正文与主题不一致；
- 是否发件人不可信。

#### 4.7.2 知识学习

需要学习：

- 邮件文件的生成、发送和接收基本过程；
- 邮件服务器端工作环境和基本原理；
- 垃圾邮件识别的基本方法和技术。

后续 agent 应采用真实的邮件服务器架构实现邮件过滤实验，整体邮件流如下：

```text
本地邮件客户端（Thunderbird / Foxmail / Outlook）
        ↓  SMTP 发信
远程 Linux 服务器：Postfix 接收邮件
        ↓
自定义 Python 邮件过滤器：解析主题 + 正文 → spam / ham
        ↓
ham：继续投递 / 转发
spam：隔离 / 标记 / 拦截
        ↓
本地邮件客户端通过 IMAP 收取过滤后的邮件
```

具体实现要点：

- **Postfix 配置**：在远程 Linux 服务器上安装并配置 Postfix 作为 MTA（邮件传输代理），接收来自本地邮件客户端的 SMTP 发送的邮件。
- **Python 过滤器集成**：Postfix 通过 `content_filter` 参数或 milter 接口将收到的邮件交给自定义 Python 程序处理。Python 程序解析邮件主题和正文，调用实验二的分类器判断 spam/ham。
- **Dovecot IMAP**：在服务器上安装 Dovecot 提供 IMAP 服务，ham 邮件投递到用户邮箱，客户端通过 IMAP 收取。
- **隔离机制**：spam 邮件移动到隔离目录（如 `/var/spool/quarantine/`），记录日志，不投递到用户收件箱。
- 如果环境限制无法搭建真实 Postfix 服务器，可采用备选方案：在本地用 Python 的 `smtpd` 模块模拟 SMTP 服务端，用 `imaplib` 相关工具模拟 IMAP 收取，但报告中应说明真实架构方案。

#### 4.7.3 动手实践

PPT 实践要求：

1. 采集垃圾邮件语料和正常邮件语料；
2. 在实验二基础上，设计一个简单的基于内容的邮件过滤系统；
3. 实现垃圾邮件和正常邮件两类语料划分；
4. 能够在邮件服务器端自动识别、监控、处理甚至拦截垃圾邮件；
5. 测试并分析系统性能。

### 4.8 推荐实现方案

#### 4.8.1 数据集格式

可以使用 CSV 格式：

```text
subject,body,label
"限时优惠","点击领取超值优惠券",spam
"课程通知","明天下午三点上课，请准时参加",ham
```

也可以使用 `.eml` 文件，然后解析邮件头和正文。

#### 4.8.2 系统模块

推荐模块：

```text
email_filter/
  data/
    raw_emails/
    train.csv
    test.csv
  src/
    parse_email.py          # 邮件解析（MIME、编码处理）
    preprocess.py           # 文本预处理（分词、去停用词）
    train_email_filter.py   # 训练垃圾邮件分类器
    email_filter.py         # Postfix content_filter / milter 入口
    evaluate.py             # 性能评估
  server_config/
    postfix_main.cf         # Postfix 配置要点
    dovecot.conf            # Dovecot IMAP 配置要点
  outputs/
    email_metrics.json
    email_predictions.csv
```

#### 4.8.3 处理流程

```text
1. 本地邮件客户端通过 SMTP 发送邮件到远程 Linux 服务器 Postfix
2. Postfix 收到邮件后，通过 content_filter 将邮件交给 Python 过滤器
3. Python 过滤器解析邮件（MIME 解码、提取 subject + body）
4. 拼接 subject + body 作为分类文本
5. 分词、去停用词、向量化
6. 使用朴素贝叶斯分类器预测类别
7. 如果为 ham：
      将邮件写回 Postfix 投递队列，继续投递到用户邮箱
   如果为 spam：
      将邮件移动到隔离目录（quarantine）
      记录拦截日志
      可选：给发件人回复退信通知
8. 用户通过 IMAP（Dovecot）连接到服务器，收取 ham 邮件
9. 输出过滤统计结果
```

#### 4.8.4 可选处理动作

对于判定为垃圾邮件的内容，可以模拟以下动作：

- 标记为垃圾邮件；
- 移动到垃圾邮件目录；
- 移动到隔离区；
- 写入日志；
- 拦截不投递；
- 给出风险分数；
- 人工复核。

建议实验中不要真正删除邮件，而是移动到隔离区，便于检查误判。

### 4.9 评价指标

邮件过滤是二分类任务，建议给出：

- Accuracy；
- Spam Precision；
- Spam Recall；
- Spam F1；
- 混淆矩阵；
- 误判样本列表。

尤其要关注：

- 正常邮件被误判为垃圾邮件：误杀，影响用户体验；
- 垃圾邮件被误判为正常邮件：漏判，影响过滤效果。

报告中应说明更重视哪一种错误。例如：

- 如果用于严肃办公场景，应降低误杀；
- 如果用于安全拦截场景，应提高垃圾邮件召回率。

### 4.10 实验报告要点

实验三报告需要说明：

- 邮件过滤系统设计和实现过程；
- 垃圾邮件与正常邮件语料来源；
- 邮件内容解析方法；
- 使用哪些内容特征；
- 是否复用实验二分类器；
- 模型训练和测试过程；
- 邮件服务器端处理流程（Postfix content_filter + Python 过滤器 + Dovecot IMAP 投递）；
- 自动识别、监控、处理或拦截方式；
- 实验结果和性能指标；
- 遇到的问题：
  - Postfix/Dovecot 配置与调试；
  - SMTP/IMAP 邮件流对接；
  - 邮件格式复杂（MIME、Base64 编码）；
  - HTML 邮件正文提取；
  - 中文编码；
  - 误杀正常邮件；
  - 垃圾邮件词汇变形；
- 解决方法；
- 最终效果。

---

## 5. 实验四：基于内容的不良短消息过滤

### 5.1 实验性质

实验四是一个**验证型实验**，要求通过编程实现一个简单的短消息过滤系统，并验证其功能和相关参数。

该实验是实验二在“短信过滤/移动终端”场景下的应用。

### 5.2 实验目的

本实验目标包括：

- 掌握短信过滤基本技术；
- 认识短信过滤技术在数字内容过滤系统中的作用；
- 了解短信内容过滤与邮件过滤的差异；
- 设计并实现一个基于内容的短信过滤系统；
- 初步完成不良短信与正常短信两类语料的划分；
- 在智能手机终端或安卓模拟器中验证过滤功能。

### 5.3 实验条件

PPT 给出的实验条件：

- 硬件：
  - 每人一台 PC 机；
  - 4–5 人合用一部智能手机。
- 软件：
  - Windows XP；
  - IE7.0；
  - Notepad 或类似文本编辑器；
  - ActivePERL 5.6；
  - Java Eclipse 3.2；
  - Microsoft Visual Studio 2008；
  - 智能手机模拟器。
- 网络：
  - 每台 PC 通过 100M 端口连接外部 Internet。

PPT 中原始方案提到 WinCE，但也给出了推荐可选实验：

> 安卓平台短信过滤；无手机实验环境时，要求在电脑安卓模拟器中实现短信过滤功能。

因此，现代实现可优先采用：

- Android Studio；
- 安卓模拟器；
- Kotlin / Java；
- 或 Python 后端 + Android 前端模拟；
- 本地短信样本输入；
- 不一定需要真实读取手机短信权限，可通过模拟输入短信文本完成分类验证。

### 5.4 背景与问题定义

短信是用户通过手机或其他电信终端直接发送或接收的文字或数字信息。PPT 中说明：

- 每条短信通常可包含 140 个英文或数字字符；
- 或 70 个中文字符；
- 垃圾短信泛滥严重，趋势甚至超过垃圾邮件；
- 垃圾短信和反动短信过滤是紧迫任务。

短信过滤主要有两类方式：

1. **基于来电号码黑名单的匹配过滤**
   - 根据号码或发送源拦截；
   - 实现简单；
   - 但号码容易变化。

2. **基于短信内容的过滤**
   - 根据短信文本内容判断；
   - 能识别来自不同号码但内容相似的不良短信；
   - 是本实验重点。

### 5.5 基于内容的短信过滤方法

PPT 指出，基于内容的垃圾短信判别方法大致可分为：

1. **基于规则的方法**
   - 关键词匹配；
   - 敏感词匹配；
   - 正则表达式；
   - 黑名单词库；
   - 号码、链接、金额、诱导词等规则。

2. **基于概率统计的方法**
   - 朴素贝叶斯；
   - 其他机器学习文本分类方法；
   - 根据训练语料学习不良短信和正常短信的统计差异。

PPT 还指出，中文短信过滤可分为：

- 主题性过滤；
- 倾向性过滤。

本实验主要关注**倾向性过滤**。

### 5.6 客户端与服务端两种处理位置

PPT 提到垃圾短信处理有两种途径：

1. **在 SMSC（短信服务中心）进行处理**
   - 属于运营商或服务中心侧处理；
   - 更集中；
   - 普通课程设计中不容易真实实现。

2. **在手机终端上编写内嵌程序实现**
   - 可个性化；
   - 部署灵活；
   - 适合智能手机应用；
   - 是课程实验更可行的方向。

后续 agent 应优先实现“手机客户端或模拟终端过滤”，而不是试图搭建真实 SMSC。

### 5.7 实验步骤

PPT 将实验四分为三个阶段。

#### 5.7.1 感性认识

需要完成：

- 收集并阅读一些不良短信；
- 讨论人们区分不良短信和正常短信的依据；
- 讨论可用于划分不良短信和正常短信的依据；
- 讨论不良短信的处理方式。

可观察特征包括：

- 是否包含广告推销；
- 是否包含诈骗诱导；
- 是否包含博彩、贷款、中奖等高风险词；
- 是否包含不良政治、暴力、色情等内容；
- 是否包含短链接；
- 是否诱导回复、转账、点击；
- 是否伪装官方机构；
- 是否语气异常急迫；
- 是否包含大量数字、联系方式或营销话术。

#### 5.7.2 知识学习

需要学习：

- 短消息倾向性分类原理；
- 智能手机终端软件环境；
- WinCE 编程技术；
- 智能手机终端短消息处理机制。

若采用 Android 替代 WinCE，应学习：

- Android 基本项目结构；
- Activity / ViewModel / UI 输入输出；
- 本地模型调用方式；
- 短信权限机制；
- 如果不读取真实短信，则实现手动输入短信文本进行过滤；
- 安卓模拟器运行和演示方法。

#### 5.7.3 动手实践

PPT 的实践要求：

1. 采集不良短消息语料和正常短信语料；
2. 在实验二基础上，用 WinCE 编程；
3. 设计实现一个简单的、运行在智能手机终端的、基于内容的短信过滤系统；
4. 初步完成不良短信和正常短信两类语料划分；
5. 测试分析系统性能。

根据 PPT 的可选实验，若无手机实验环境，可改为：

1. 采集不良短信和正常短信样本；
2. 训练或复用实验二文本分类器；
3. 在电脑安卓模拟器中实现短信过滤功能；
4. 在 App 中输入或模拟接收短信；
5. 显示分类结果：
   - 正常短信；
   - 不良短信；
   - 风险分数；
   - 建议处理动作；
6. 测试准确率和误判情况。

### 5.8 推荐实现方案

#### 5.8.1 最小可行版本

如果时间有限，可以实现一个最小可行版本：

```text
Android 模拟器 App
  ↓
用户在输入框粘贴短信文本
  ↓
点击“检测”
  ↓
调用本地规则或模型
  ↓
显示“正常短信 / 不良短信”
  ↓
显示命中的关键词或分类概率
```

该版本不需要真实读取短信权限，演示更稳定。

#### 5.8.2 规则 + 朴素贝叶斯混合方案

可以采用两级判断：

1. **规则层**
   - 命中明显高危词、链接、号码模式；
   - 快速给出风险分。

2. **模型层**
   - 对未被规则明确判断的短信调用朴素贝叶斯分类器；
   - 输出正常 / 不良。

流程：

```text
短信文本
  ↓
文本清洗
  ↓
规则匹配
  ↓
若高危规则命中 → 不良短信
  ↓
否则进入朴素贝叶斯分类器
  ↓
输出分类结果
```

#### 5.8.3 数据格式

短信数据可保存为：

```text
text,label
"恭喜你获得大奖，请点击链接领取",bad
"老师您好，明天下午实验课正常上课",normal
```

或者：

```json
[
  {
    "text": "低息贷款快速放款，点击链接申请",
    "label": "bad"
  },
  {
    "text": "晚上记得带钥匙",
    "label": "normal"
  }
]
```

#### 5.8.4 Android 端工程建议

```text
sms_filter_android/
  app/
    src/main/java/...
      MainActivity.kt
      SmsClassifier.kt
      RuleFilter.kt
    src/main/assets/
      keywords.txt
      model_config.json
  data/
    sms_train.csv
    sms_test.csv
  docs/
    screenshots/
```

如果模型较复杂，可在 PC 端训练，Android 端只加载：

- 词表；
- 关键词规则；
- 类别先验概率；
- 条件概率表；
- 或导出的轻量模型参数。

### 5.9 评价指标

短信过滤是二分类任务，建议给出：

- Accuracy；
- 不良短信 Precision；
- 不良短信 Recall；
- 不良短信 F1；
- 混淆矩阵；
- 误判样本分析；
- App 运行截图；
- 安卓模拟器演示过程。

短信场景尤其需要关注：

- 短文本特征稀疏；
- 隐晦表达；
- 谐音和变形词；
- URL、电话、金额等非自然语言特征；
- 正常通知短信与营销短信之间的边界；
- 误判对用户体验的影响。

### 5.10 实验报告要点

实验四报告需要说明：

- 短信过滤系统设计和实现过程；
- 不良短信和正常短信语料来源；
- 数据预处理方法；
- 是否复用实验二分类器；
- 是否加入规则过滤；
- 手机端或模拟器端实现方式；
- 分类流程；
- 系统界面和运行截图；
- 测试结果和性能指标；
- 遇到的问题：
  - 短文本信息少；
  - 关键词变形；
  - 安卓权限或模拟器问题；
  - 模型部署问题；
  - 误判正常短信；
- 解决方法；
- 最终效果。

---

## 6. 四个实验的关系与推荐工程复用方式

### 6.1 数据流关系

```text
Web 爬虫采集评论/文章
        ↓
形成文本语料
        ↓
分词、清洗、标注
        ↓
训练文本倾向分类器
        ↓
复用到邮件正文过滤
        ↓
复用到短信内容过滤
```

### 6.2 模块复用建议

可以将通用能力抽成公共模块：

```text
common/
  text_preprocess.py       # 文本清洗、分词、停用词
  vectorizer.py            # VSM / TF-IDF
  naive_bayes.py           # 朴素贝叶斯分类器
  metrics.py               # 评估指标
  io_utils.py              # 文件读写
```

实验一单独实现：

```text
crawler/
  crawl.py
  parse.py
  urls.txt
```

实验二实现训练与评估：

```text
sentiment_classifier/
  train.py
  predict.py
  evaluate.py
```

实验三调用实验二分类器：

```text
email_filter/
  parse_email.py
  classify_email.py
```

实验四调用实验二分类器或导出轻量参数：

```text
sms_filter/
  sms_classifier.py
  android_app/
```

### 6.3 推荐总目录

```text
digital_content_filter/
  README.md
  requirements.txt

  data/
    raw/
    processed/
    experiment1_web/
    experiment2_text/
    experiment3_email/
    experiment4_sms/

  common/
    preprocess.py
    feature.py
    nb_classifier.py
    metrics.py

  experiment1_crawler/
    urls.txt
    crawler.py
    outputs/

  experiment2_text_sentiment/
    train.py
    predict.py
    evaluate.py
    outputs/

  experiment3_email_filter/
    parse_email.py
    train_email_filter.py
    email_filter.py         # Postfix content_filter 入口
    evaluate.py
    server_config/           # Postfix + Dovecot 配置参考
    outputs/

  experiment4_sms_filter/
    sms_filter.py
    android_demo/
    outputs/

  reports/
    实验一报告.md
    实验二报告.md
    实验三报告.md
    实验四报告.md
```

---

## 7. 面向后续 agent 的任务边界说明

### 7.1 实验一的边界

实验一只要求实现简单网络爬虫，重点是：

- 读取 URL；
- 获取网页；
- 保存网页或文本；
- 验证保存结果能正常显示。

不要过度扩展为复杂分布式爬虫，不要绕过网站访问限制，不要采集隐私或高风险数据。

### 7.2 实验二的边界

实验二的核心是：

- 中文文本倾向分类；
- VSM 或 TF-IDF；
- Naïve Bayes；
- 特征阈值调整；
- 分类性能评估。

不要直接跳到大型深度学习模型，除非用户明确要求。PPT 的重点是传统文本分类和朴素贝叶斯。

### 7.3 实验三的边界

实验三的核心是：

- 垃圾邮件 / 正常邮件二分类；
- 基于内容；
- 可在服务器端或模拟服务器端自动处理；
- 测试系统性能。

可以用本地目录模拟邮件服务器，不强制接入真实邮件服务器。

### 7.4 实验四的边界

实验四的核心是：

- 不良短信 / 正常短信二分类；
- 基于内容；
- 倾向性过滤；
- 手机终端或安卓模拟器演示。

若无真实手机或短信权限，可用安卓模拟器 + 手动输入短信文本实现演示。

### 7.5 报告共同要求

每个实验报告都应说明：

- 实验目的；
- 实验原理；
- 实验环境；
- 实验内容；
- 系统设计；
- 关键步骤；
- 核心代码逻辑；
- 参数设置；
- 实验结果；
- 遇到的问题；
- 解决方法；
- 效果分析；
- 总结。

---

## 8. 四个实验的最简验收清单

### 实验一验收清单

- [ ] 有 URL 列表文件；
- [ ] 程序能读取 URL；
- [ ] 程序能请求网页；
- [ ] 程序能保存网页或文本到本地目录；
- [ ] 保存文件无明显乱码；
- [ ] 有运行日志；
- [ ] 能展示采集结果。

### 实验二验收清单

- [ ] 有中文文本训练集和测试集；
- [ ] 有明确类别标签；
- [ ] 有分词和预处理；
- [ ] 有 VSM / TF-IDF 表示；
- [ ] 有 Naïve Bayes 分类器；
- [ ] 有多轮参数或特征阈值实验；
- [ ] 有准确率、召回率、F1；
- [ ] 有错误样本分析。

### 实验三验收清单

- [ ] 有垃圾邮件和正常邮件语料；
- [ ] 能解析邮件主题和正文；
- [ ] 能调用分类器判断 spam / ham；
- [ ] 能模拟服务器端监控或处理；
- [ ] 能把垃圾邮件标记、移动或隔离；
- [ ] 有性能评估结果；
- [ ] 有误判分析。

### 实验四验收清单

- [ ] 有不良短信和正常短信语料；
- [ ] 能对短信文本进行预处理；
- [ ] 能分类 normal / bad；
- [ ] 有规则或模型说明；
- [ ] 有安卓模拟器或终端演示；
- [ ] 能展示输入短信后的分类结果；
- [ ] 有性能评估结果；
- [ ] 有误判分析和截图。

---

## 9. 推荐给 agent 的实现优先级

如果时间有限，应按以下优先级推进：

1. **先完成实验一最小可运行爬虫**  
   能采集公开中文文本并保存即可。

2. **完成实验二文本分类器**  
   这是后续邮件和短信过滤的核心。

3. **将实验二分类器包装成可调用接口**  
   例如 `predict(text) -> label, probability`。

4. **实验三用本地邮件目录模拟服务器端过滤**  
   不必真实接入邮件服务器。

5. **实验四用安卓模拟器或桌面模拟界面完成短信过滤演示**  
   不必真实读取短信，手动输入或样本列表演示即可。

6. **最后补充报告、截图、指标和错误分析**。

---

## 10. 关键提醒

- PPT 中部分软件环境较旧，如 Windows XP、IE、WinCE、ActivePERL 等。实际完成时可使用现代等价工具，但报告中应说明替代关系。
- 课程实验重点不是追求最高模型精度，而是理解数字内容过滤的完整流程。
- 实验二是四个实验中的算法核心，实验三和实验四应尽量复用实验二的分类器或特征处理模块。
- 实验一采集的数据应尽量选择评论类、书评类或其他便于倾向分类的中文文本，以服务实验二。
- 爬虫实验必须注意合法合规，不应绕过访问控制或抓取隐私数据。
- 邮件和短信过滤实验应重视误判分析，因为过滤系统的实用性不仅取决于准确率，也取决于误杀率和漏判率。
